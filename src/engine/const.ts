export enum ALIGN {
    LEFT_TOP,
    LEFT_CENTER,
    LEFT_BOTTOM,
    CENTER_TOP,
    CENTER,
    CENTER_BOTTOM,
    RIGHT_TOP,
    RIGHT_CENTER,
    RIGHT_BOTTOM
}

export enum ORIENTATION {
    LANDSCAPE = 'landscape-primary',
    PORTRAIT = 'portrait-primary'
}